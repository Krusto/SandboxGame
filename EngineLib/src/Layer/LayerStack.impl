#include "LayerStack.hpp"

namespace Engine
{
    template <typename T, typename... Args>
    void LayerStack::ConstructAndPushLayer(Args&&... args)
    {
        LayerStack::PushLayer(Allocator::Allocate<T>(std::forward<Args>(args)...));
    }

}// namespace Engine